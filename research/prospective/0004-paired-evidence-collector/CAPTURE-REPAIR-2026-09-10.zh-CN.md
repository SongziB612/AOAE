# 行情采集恢复（2026-09-10）

故障证据：20:46 的旧运行六份 CSV 中，159915 截止 9 月 9 日，其余五份截止 9 月 10 日；没有生成完整 manifest，任务退出码为 1。没有使用不完整数据推进账本。

本轮改动：新增有界采集入口 `scripts/run_paired_evidence_cycle.py`，原 Windows 任务调用的 `daily_paired_evidence.ps1` 转发到此入口。最多三次抓取，间隔 10 秒，每次抓取进程最多 90 秒；收集与现金核对阶段各最多 60 秒。每次抓取使用独立目录，不拼接不同轮次数据、不覆盖失败 CSV。

每个阶段保存 stdout、stderr、起止时间和退出码；每轮保存六只 ETF 的实际行情日期；整个运行保存 `cycle-status.json`。只有日期齐备且抓取成功，才进入原哈希与价格校验，再更新现金账本。收集或账本失败不自动重试写账，保留报错供审查。日期初筛不是公司行动或数据真实性审查。

实际运行：`20260910T134216045117Z-a7cad2e7` 第一轮成功，六只 ETF 全部止于 9 月 10 日，最终状态 `CASH_CHECKPOINT_PASS`。这不证明首次失败由重试代码解决：本次运行时数据商已经更新齐备。模拟的“先失败后成功”“三次失败耗尽”“立即成功”“非法预算”路径由新增测试验证。

旧失败目录保留，无用户文件删除。下一次原任务启动会使用新入口；本次验证是显式执行同一入口，未声称任务调度器的旧失败退出码已经被更新。

限制：电脑必须开机并登录，网络需可用；数据商长时间滞后时三次重试仍可能失败，不保证补齐。系统被强制终止、磁盘写入失败时不保证能生成最终状态文件；阶段已有日志尽量保留。自动任务依然只执行公开数据采集与现金核对，不运行正式信号或下单。

经济判定：本轮恢复的是数据完整性，不产生 alpha 或新增盈利证据。策略与本金授权未改变。
