# 新候选已冻结，尚未启动前向采集

`freeze.json` 保存实际 UTC 冻结时间及17个代码/输入文件哈希。比较三组30万元模拟组合：修正账务后的模型、同五只ETF月度等权、匹配模型总仓位的等权对照。更改已绑定的模型或输入会使完整性检查失败；应另开版本，不拼接成绩。

本轮只读核查现有 Windows 任务 `AOAE_ETF_Paper_EOD`：状态Ready，上次退出码0，漏跑计数0；实际命令仍运行 `scripts/daily_paper_cycle.ps1`。该脚本只覆盖旧的0001、0003、0004模拟账户，不包含本候选。不能把旧任务成功当成新模型已积累证据。

当前新候选信号0、成交0、实盘授权关闭。尚需接入实际时间戳信号留存、新原始行情与公司行动核验、三组独立前向账本、成交证据接纳器。冻结文件不是可运行的全套前向系统，现有任务未修改。月末必须事先留下信号，错过的信号不能事后补算成已观察成交；满一年也不自动获得资本资格。

核查：`.venv/python.exe scripts/freeze_300k_candidate.py --verify research/prospective/0003-raw-price-300k-paired-forward/freeze.json`。
