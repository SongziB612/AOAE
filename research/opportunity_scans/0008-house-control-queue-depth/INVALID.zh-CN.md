# SCAN-0008 结果无效说明

`result.json` 的逐腿订单簿、历史流量和清空时间计算可读，但汇总选择瓶颈时错误地把合法的 `0.0` 当成缺失值，导致将 `Balance Other — YES` 错标为瓶颈。

真实最大逐腿数值来自 `Democratic House control — NO`，为 2907.2348 天。该错误在发现后未静默覆盖原结果；修复代码后使用新的 SCAN-0009 重新采集和判定。
