# 当前 30 万元候选资金前验收：未通过

本文件只针对 PRO-0003-raw-price-300k-paired-forward。早期 3,000 元试验的 `all_controllable_preparation_complete=true` 不适用于本候选，不覆盖它仍缺少的工程与证据。旧报告保留，不修改历史记录。

## 本轮实际新增

- `presave_reviewed_signal.py`：从有完整来源与时点元数据的审查包读取六只 ETF 历史收盘、公司行动、交易日历，校验摘要与文件范围，按冻结模型重新计算三组权重，检查月末与下一交易日以及保存窗口后写入隔离账本。
- `run_reviewed_shadow_event.py` 不再直接接收外部手填的 PRESAVE_SIGNAL；改由上述计算入口保存，避免声明“已审查”但权重不是冻结模型的结果。
- 新日历边界测试和完整合成审查包测试通过，包含迟到拒绝及摘要不匹配拒绝。全套 255 项测试通过。合成包不是现实来源核验。
- `audit_current_candidate.py` 生成当前候选专用快照，核对冻结完整性、历史结果来源摘要、两个账本以及最近采集状态，并把内部未完成项与外部确认分开。当前决策 `DO_NOT_FUND`。

## 我仍需完成的内部工作

1. 真实资料的独立内容核验与完整公司行动、交易日历审查包。声明和哈希不能替代事实核验。
2. 将已审查的每日资料生成、正式信号生成、持仓事件记账和月度比较接入定时流程。现在定时任务只采集和核对现金。
3. 可执行性证据接入和经济准入规则完整验收，不能以开盘价全额模拟成交替代测得的成交条件。

这些不是用户造成的阻塞，也不是单纯等待即可解决，不能标为可控准备完成。

## 无法凭代码完成的外部证据

- 实际账户佣金及证券权限确认；官网费率上限不是个人实际费率。
- 新数据上的收益与风险证据。冻结方案要求首个合格预存信号之后按既定评价期观察，不能用旧回测冒充时间已经经过，也不能将旧方案的 90 天门槛套到新方案。
- 真正投入资金前的新授权、金额、损失承受与资金归属确认；历史对话中的 30 万元仅作为当前模拟参数，不是新的下单许可。

## 输入包约定与边界

审查包包含 `signal_day`、`prices`（六只代码到 CSV 相对路径）、`calendar`（sessions 列表 JSON）、`actions`（coverage_start、coverage_end、status、actions 列表 JSON）及 `review`。review 必须包含 source_sha256、source_provenance、reviewed_at、status。来源字段沿用 `source_timing.py` 的逐文件时点契约。

程序只检查输入结构、摘要、声明时间及计算一致性；不自行认定来源声明真实，不审定官方日历或基金公告是否完整，不代表整个交易执行系统通过审计。此生成入口不与旧现金任务自动联动，不新增实盘订单。

## 第一性原理结论

已有历史经济结果没有新增独立优势证据，付款方与破产概率尚未确立；现金账户仍为零信号、零模拟成交。当前不批准投入资金，不给出保证开始盈利的日期。

机器快照：`current-300k-2026-09-10-v1.json`。本文件是未完成项的明确记录，不是完工声明。
